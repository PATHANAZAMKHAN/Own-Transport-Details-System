<!doctype html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>OWN TRANSPORT MEMO SERVICE</title>
  <link rel="stylesheet" href="/style.css" />
  <link rel="shortcut icon" href="/Transport-Truck.ico" type="image/x-icon" />

</head>

<body>
<!-- <body onbeforeunload="cacheValue()" onload="populateCachedValue()"> -->
  <!-- <script>
    function cacheValue() {

      const form = document.getElementById('main');

      const formData = new FormData(form);
      for (const pair of formData.entries()) {
        sessionStorage.setItem(pair[0], pair[1])
        console.log(pair[0], pair[1]);

      }
    }

    function populateCachedValue() {
      const form = document.getElementById('main');

      for (const key in sessionStorage) {
        const element = form.elements.namedItem(key);
        if (element) {
          element.value = sessionStorage.getItem(key);
        }
      }
    }

    function clearCache() {
      sessionStorage.clear()

      const form = document.getElementById('main');

      const formData = new FormData(form);
      for (const pair of formData.entries()) {
        const element = form.elements.namedItem(pair[0]);
        element.value = ""

      }
      
    }
  </script> -->
  <nav id="navbar">
    <ul id="navigation">
      <li class="nav_items"><a href=<%=OTSMEMOURL %>>OTS MEMO</a></li>
      <li class="nav_items"><a href=<%=OTSSLIPURL %>>OTS SLIP</a></li>
      <li class="nav_items"><a href=<%=VIJAYANDRAMEMOURL %>>VARL MEMO</a></li>
      <li class="nav_items"><a href=<%=VIJAYANDRASLIPURL%>>VARL SLIP</a></li>
      </li>
    </ul>
  </nav>

  <form id="main" method="post" action=<%=OTSMEMOURL %>>
    <h2 id="heading">OWNER TRANSPORT MEMO GENERATOR</h2>

    <%- include ('common_memo') %>


  </form>
</body>

</html>